databaseChangeLog:
  - changeSet:
      id: add-status-to-users-table
      author: annov4
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: status
                  type: VARCHAR(50)
                  constraints:
                    nullable: false
                    defaultValue: 'active'